# Linked stress chapter

## Continuation

This second Markdown document is used to measure document-cache reuse and
bounded multi-document history. It contains enough prose to force pagination
without changing the link target semantics.

The page cache may evict this chapter while the reader visits another
document, but Back and Forward must restore it without reparsing when it is
still inside the configured document-cache window.
