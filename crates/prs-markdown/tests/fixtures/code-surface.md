# Code-block surface

The surface stays stable for short, long, wrapped, and blank source lines.

```rust
let short = 1;

let long_name = "This source line is intentionally long enough to wrap on the reader page and keep its code surface aligned with every other line";
let after = 2;
```
