# Reader font size

The reader keeps this passage in view while the body text grows or shrinks. The words wrap at different points, but the document remains readable and the heading keeps its proportional emphasis.

## Linked reading

[Follow the linked passage](#destination) and verify that the link remains easy to activate at the minimum and maximum settings.

### Destination

This destination is deliberately long enough to show the relationship between body text and headings in the host goldens. Repeat the stable passage so the page has useful variation at both font-size extremes.

The reader rebuilds pagination from the same source text after every settings change. This keeps links, lists, and other touch targets aligned with the rendered content.

- First list item with readable text.
- Second list item with more readable text.
