[package]
name = "prs-sync-protocol"
version = "0.1.0"
edition = "2021"
description = "Native and Cloudflare Worker wire types for PRSync"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
