name: PRSync Cloudflare production deployment

on:
  workflow_run:
    workflows:
      - CI
    types:
      - completed
    branches:
      - main

permissions:
  actions: read
  contents: read

concurrency:
  group: prs-cloudflare-production
  cancel-in-progress: false

env:
  WORKER_BUILD_VERSION: "0.8.6"
  WRANGLER_VERSION: "4.135.0"

jobs:
  change-gate:
    name: Check for production deployment changes
    if: >-
      ${{ github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'push' &&
      github.event.workflow_run.head_branch == 'main' &&
      github.event.workflow_run.head_repository.full_name == github.repository }}
    runs-on: ubuntu-24.04
    outputs:
      deploy_api: ${{ steps.change-detection.outputs.deploy_api }}
      deploy_reader_web: ${{ steps.change-detection.outputs.deploy_reader_web }}
    steps:
      - name: Check out the tested main commit and history
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
          fetch-depth: 0

      - name: Find the previous successful main CI commit
        id: previous-ci
        env:
          GH_TOKEN: ${{ github.token }}
          TESTED_SHA: ${{ github.event.workflow_run.head_sha }}
        shell: bash
        run: |
          set -euo pipefail
          base_sha=''
          if candidate_shas=$(
            gh api --paginate \
              -H 'Accept: application/vnd.github+json' \
              "/repos/${GITHUB_REPOSITORY}/actions/workflows/ci.yml/runs?branch=main&status=completed&per_page=100" \
              --jq '.workflow_runs[] | select(.conclusion == "success") | .head_sha'
          ); then
            while IFS= read -r candidate; do
              [[ "$candidate" == "$TESTED_SHA" ]] && continue
              if git merge-base --is-ancestor "$candidate" "$TESTED_SHA"; then
                base_sha="$candidate"
                break
              fi
            done < <(printf '%s\n' "$candidate_shas")
          else
            printf '%s\n' 'could not query prior CI runs; the gate will deploy conservatively'
          fi
          printf 'base_sha=%s\n' "$base_sha" >> "$GITHUB_OUTPUT"
          if [[ -n "$base_sha" ]]; then
            printf 'previous_successful_ci_sha=%s\n' "$base_sha"
          else
            printf '%s\n' 'previous_successful_ci_sha=unknown; the gate will deploy conservatively'
          fi

      - name: Detect Cloudflare deploy-relevant changes
        id: change-detection
        env:
          BASE_SHA: ${{ steps.previous-ci.outputs.base_sha }}
          TESTED_SHA: ${{ github.event.workflow_run.head_sha }}
        run: |
          set -euo pipefail
          gate_args=(
            --repo-root "$GITHUB_WORKSPACE"
            --head "$TESTED_SHA"
            --github-output "$GITHUB_OUTPUT"
            --summary "$GITHUB_STEP_SUMMARY"
          )
          if [[ -n "$BASE_SHA" ]]; then
            gate_args+=(--base "$BASE_SHA")
          fi
          python3 tools/prs-cloudflare-deploy-gate.py "${gate_args[@]}"

  deploy:
    name: Build and deploy production Workers
    needs: change-gate
    if: >-
      ${{ needs.change-gate.result == 'success' &&
      (needs.change-gate.outputs.deploy_api == 'true' ||
      needs.change-gate.outputs.deploy_reader_web == 'true') &&
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'push' &&
      github.event.workflow_run.head_branch == 'main' &&
      github.event.workflow_run.head_repository.full_name == github.repository }}
    runs-on: ubuntu-24.04
    environment:
      name: production
    env:
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      PRS_READER_URL: ${{ vars.PRS_READER_URL }}
    steps:
      - name: Check out the tested main commit
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Read production build pins
        if: needs.change-gate.outputs.deploy_api == 'true'
        id: build-pins
        shell: bash
        run: |
          tools/prs-t1-agent-build.sh print-github-actions >> "$GITHUB_OUTPUT"
          printf 'manifest_hash=%s\n' "$(tools/prs-t1-agent-build.sh print-manifest-hash)" >> "$GITHUB_OUTPUT"

      - name: Install the pinned Rust toolchain
        if: needs.change-gate.outputs.deploy_api == 'true'
        run: |
          rustup toolchain install "${{ steps.build-pins.outputs.rust_toolchain }}" \
            --profile minimal \
            --target wasm32-unknown-unknown \
            --no-self-update
          rustup run "${{ steps.build-pins.outputs.rust_toolchain }}" rustc --version
          rustup run "${{ steps.build-pins.outputs.rust_toolchain }}" cargo --version

      - name: Cache Worker Rust dependencies
        if: needs.change-gate.outputs.deploy_api == 'true'
        uses: Swatinem/rust-cache@63fed3e2fecf6f7b51dc6f043341b79ef82a9ae7 # v2.9.2
        with:
          shared-key: prs-cloudflare-production
          add-job-id-key: false
          key: >-
            cloudflare-rust-${{ steps.build-pins.outputs.rust_toolchain }}-
            manifests-${{ steps.build-pins.outputs.manifest_hash }}
          cache-targets: true
          cache-all-crates: true
          cache-bin: true

      - name: Install pinned worker-build
        if: needs.change-gate.outputs.deploy_api == 'true'
        run: cargo +"${{ steps.build-pins.outputs.rust_toolchain }}" install worker-build --version "$WORKER_BUILD_VERSION" --locked

      - name: Install pinned Wrangler
        run: npm install --global "wrangler@$WRANGLER_VERSION"

      - name: Build the production Worker
        if: needs.change-gate.outputs.deploy_api == 'true'
        working-directory: crates/prs-cloudflare
        run: worker-build --release

      - name: Read browser reader build pins
        if: needs.change-gate.outputs.deploy_reader_web == 'true'
        id: reader-web-build-pins
        run: tools/reader-web-build.sh print-github-actions >> "$GITHUB_OUTPUT"

      - name: Install the pinned browser reader Rust toolchain
        if: needs.change-gate.outputs.deploy_reader_web == 'true'
        run: |
          rustup toolchain install "${{ steps.reader-web-build-pins.outputs.rust_toolchain }}" \
            --profile minimal \
            --target "${{ steps.reader-web-build-pins.outputs.target }}" \
            --no-self-update
          rustup run "${{ steps.reader-web-build-pins.outputs.rust_toolchain }}" rustc --version
          rustup run "${{ steps.reader-web-build-pins.outputs.rust_toolchain }}" cargo --version

      - name: Cache browser reader Rust dependencies
        if: needs.change-gate.outputs.deploy_reader_web == 'true'
        uses: Swatinem/rust-cache@63fed3e2fecf6f7b51dc6f043341b79ef82a9ae7 # v2.9.2
        with:
          shared-key: prs-reader-web-production
          add-job-id-key: false
          cache-targets: true
          cache-all-crates: true
          cache-bin: true

      - name: Install pinned wasm-bindgen CLI
        if: needs.change-gate.outputs.deploy_reader_web == 'true'
        run: |
          cargo +"${{ steps.reader-web-build-pins.outputs.rust_toolchain }}" install \
            wasm-bindgen-cli \
            --version "${{ steps.reader-web-build-pins.outputs.wasm_bindgen_version }}" \
            --locked
          wasm-bindgen --version

      - name: Build and validate the browser reader static assets
        if: needs.change-gate.outputs.deploy_reader_web == 'true'
        env:
          RUSTUP_TOOLCHAIN: ${{ steps.reader-web-build-pins.outputs.rust_toolchain }}
        run: |
          tools/reader-web-build.sh build
          tools/test-reader-web.sh target/reader-web

      - name: Enable sanitized Wrangler debug logging
        if: ${{ runner.debug == '1' }}
        shell: bash
        run: echo 'WRANGLER_LOG=debug' >> "$GITHUB_ENV"

      - name: Deploy browser reader to the existing prs-reader-web Worker
        if: needs.change-gate.outputs.deploy_reader_web == 'true'
        env:
          WRANGLER_LOG_SANITIZE: "true"
        run: tools/prs-reader-web-deploy.sh --production

      - name: Upload and promote production version, then verify schema readiness
        if: needs.change-gate.outputs.deploy_api == 'true'
        env:
          WRANGLER_LOG_SANITIZE: "true"
        run: tools/prs-cloudflare-deploy.sh --production

      - name: Verify deployed Worker health
        if: needs.change-gate.outputs.deploy_api == 'true'
        run: |
          curl --fail-with-body --silent --show-error --max-time 10 \
            "${PRS_READER_URL%/}/health"

      - name: Verify browser assets and exact-origin API CORS
        if: >-
          ${{ needs.change-gate.outputs.deploy_api == 'true' ||
          needs.change-gate.outputs.deploy_reader_web == 'true' }}
        run: tools/prs-reader-web-verify-production.sh
