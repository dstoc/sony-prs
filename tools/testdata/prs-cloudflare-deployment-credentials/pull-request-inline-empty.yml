name: Inline pull request deployment workflow

on: {push: {branches: [main]}, pull_request: {}}

jobs:
  deploy:
    environment: production
    env:
      ACCOUNT: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
    runs-on: ubuntu-24.04
