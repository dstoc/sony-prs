name: Bracket notation deployment workflow

on:
  push:
    branches:
      - main

jobs:
  deploy:
    environment:
      name: production
    env:
      ACCOUNT: ${{ secrets['CLOUDFLARE_ACCOUNT_ID'] }}
      TOKEN: ${{ secrets["CLOUDFLARE_API_TOKEN"] }}
    runs-on: ubuntu-24.04
