# Linked chapter

## Details

This document was opened through a relative Markdown link from the demo
README. Its heading is an anchor target, so the browser exercises document and
fragment navigation together.

![Chapter detail](../assets/detail.png)

The chapter image is loaded from the selected directory snapshot. Follow the
[field notes](notes.md) for another internal document, or use the
[README link](../README.md) to return through a new navigation event.

The [project website](https://example.com/prs-t1-demo) is intentionally
external. The shared reader emits an external-link event and the browser
shows the URL in its status line without opening a new page.
