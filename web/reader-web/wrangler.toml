# This is a static-assets-only Worker. The browser API remains on prs-reader.
name = "prs-reader-web"
compatibility_date = "2026-09-23"
workers_dev = true

[assets]
directory = "../../target/reader-web"
# The current reader has one entry page and no client-side URL routes.
not_found_handling = "none"
